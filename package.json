{
  "name": "monolith",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript IaC for Cloudflare Workers — M1 scaffold (not published)",
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc -p tsconfig.run.json",
    "test": "pnpm --filter @monolith/core test && pnpm --filter @monolith/cloudflare test && pnpm --filter @monolith/cli test",
    "test:integration": "pnpm --filter @monolith/cli test:integration",
    "monolith": "node packages/cli/bin/monolith.js"
  },
  "devDependencies": {
    "@monolith/cloudflare": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
