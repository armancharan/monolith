# Monolith audit — 2026-06-07

Condensed from strategic/technical audit (task `970638bf`). Full analysis lives in agent transcript; this doc captures tables and follow-ups.

**Posture:** Breadth over M1-only framing; honest split between strategy docs and shipped code.

---

## Executive summary

Monolith has a solid **M1 foundation**: import → local state → plan → wrangler deploy → typegen. Automated coverage was **unit-only** with **zero integration or live-cloud tests**. **`monolith.run.ts` was not wired** into desired state; deploy did not consume plan output.

**Follow-ups in this commit:** fixture integration tests, `monolith.run.ts` desired-state merge, deploy plan guard (`--auto-approve`).

---

## Test inventory (pre-follow-up)

| Category | Count | Live-cloud |
|----------|------:|:----------:|
| Unit / component | 8 files, ~30 cases | N |
| Integration (CLI flow, no CF) | 0 | N |
| Live-cloud smoke | 0 | N |
| Golden fixture | 1 (`dogfood-wrangler.jsonc`) | N |
| Manual dogfood | 1 (external repo) | Y |

| File | Type | Proves |
|------|------|--------|
| `packages/core/src/plan.test.ts` | Unit | Plan diff engine |
| `packages/core/src/state.test.ts` | Component | State R/W, stage isolation |
| `packages/cloudflare/src/client.test.ts` | Unit | CF client (mocked fetch) |
| `packages/cloudflare/src/auth.test.ts` | Component | Auth precedence |
| `packages/cloudflare/src/wrangler-import.test.ts` | Golden | Wrangler parse + codegen |
| `packages/cloudflare/src/typegen.test.ts` | Unit | `MonolithEnv` generation |
| `packages/cli/src/deploy.test.ts` | Component | Deploy shell (mocked wrangler) |
| `packages/cli/src/typegen.test.ts` | Component | CLI typegen |

**Gaps:** no multi-command CLI harness, no live wrangler/CF, no cloud drift detection, no CI.

*(Updated 2026-06-07 follow-up: CI wired, live scaffold added, DO/Queue stack DSL, partial cloud drift read in plan, `monolith dev` implemented.)*

---

## API coverage matrix (summary)

| Resource | Support | Monolith | Wrangler | CF REST |
|----------|---------|----------|----------|---------|
| Worker | Partial | State + deploy delegate | Full deploy | None |
| D1 / KV | Partial | Import, plan, typegen | Attach on deploy | None |
| R2 / Queue | Partial | Parse + typegen | Attach | None |
| Durable Objects | Partial | Stack DSL + import | Wrangler | Partial read via settings API |
| Auth / whoami | Full | Env + OAuth TOML | Shared login | `/user`, `/accounts` |
| Plan | Partial | Local state vs desired + optional cloud hints | Re-parse file | Settings API + wrangler deployments |
| Destroy / init | Stub | Message only | Manual | None |
| `monolith.run.ts` | Was stub | **Now merged into plan desired state** | — | — |

Only wrangler subprocess used for deploy: `npx wrangler deploy [--config path]`.

---

## Reconcile gap

**Target (product spec):**

```
desired := eval(monolith.run.ts, stage)
actual  := provider.Read(stage)
plan    := diff(actual, desired)
deploy  := apply(plan) with guardrails
```

**Before this commit:**

```
desired := parse(wrangler.jsonc)     // monolith.run.ts unused
actual  := local state file          // not cloud
plan    := diff(state, desired)
deploy  := wrangler deploy           // ignores plan
```

**After this commit (incremental):**

- Desired: `monolith.run.ts` binding declarations **merged with** wrangler/import IDs (when run file present)
- Deploy: blocks if plan has changes unless `--auto-approve`
- Still missing: true apply/reconcile (cloud read now partial in plan)

---

## Recommended test pyramid

| Layer | Scope | Status |
|-------|--------|--------|
| **L1 — Fixture integration** | import → plan → deploy (mock wrangler), no network | **Added** (`packages/cli/tests/integration/`) |
| **L2 — Provider smoke** | Real token: whoami; ephemeral deploy + HTTP | Deferred (`MONOLITH_LIVE_TESTS=1`) |
| **L3 — Dogfood e2e** | External repo AC-1..5 in CI | Deferred (manual today) |
| **L4 — Reconcile** | Cloud drift, DO migrations, apply consumes plan | Partial — cloud binding hints in plan; full reconcile post-M1 |

Suggested CI (future):

```yaml
jobs:
  unit:        { steps: [pnpm test] }
  integration: { steps: [pnpm test:integration] }
  live-smoke:  { if: schedule, env: cloudflare, steps: [pnpm test:live] }
```

---

## Capability checklist (C0–C10)

| ID | Capability | Actual (audit date) |
|----|------------|---------------------|
| C0 | Scaffold / init | Partial — `init` stub |
| C1 | Wrangler import | Done |
| C2 | CF API + auth | Partial — whoami only |
| C3–C4 | Worker + bindings | Partial — D1/KV strong; R2/Queue parse |
| C5 | State engine | Done |
| C6 | Plan engine | Done — local diff |
| C7 | Deploy | Partial — wrangler delegate; plan guard added |
| C8 | Typegen | Done |
| C9 | CLI | Mostly done — init/destroy stubs; `dev` wired |
| C10 | Demo script | Friction done; demo missing |

---

## Highest-leverage deferred work

1. ~~Live-cloud smoke in CI (optional secret gate)~~ — scaffold + workflow stub **done**; enable schedule/secrets to activate
2. ~~Cloud read for drift (dashboard edits → plan)~~ — **partial** (`monolith plan --cloud` / auto when authed)
3. ~~Queue + DO in stack DSL and reconcile~~ — stack DSL **done**; full reconcile deferred
4. ~~`dev` / preview environments~~ — `monolith dev` **done**; preview envs deferred
5. ~~GitHub Actions CI wiring~~ — **done** (unit + integration on PR)
6. Remote state + audit trail (commercial path)
7. `destroy` command (full teardown)
